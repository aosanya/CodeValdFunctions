# CodeValdFunctions — Completed MVP Tasks

| Task ID | Title | Completed | Branch | Notes |
|---------|-------|-----------|--------|-------|
| MVP-FN-001 | Service Scaffolding | 2026-05-20 | main | gRPC shell, Cross heartbeat registrar, FunctionsService proto skeleton, health check |
| MVP-FN-002 | Job Entity Schema | 2026-05-20 | main | `DefaultFunctionsSchema()`, ArangoDB backend, Job struct, schema seed on startup |
| MVP-FN-003 | Job Lifecycle & CRUD | 2026-05-20 | main | `FunctionsManager` interface + `functionsManager` impl; CreateJob/StartJob/CompleteJob/FailJob/CancelJob; state machine enforced; 14 unit tests pass |
| MVP-FN-004 | Event Subscription | 2026-05-20 | main | `EventReceiverServer` (NotifyEvent dedup + ReceivedEvent write); `FunctionsDispatcher` creates+runs Job async; `ReceivedEventTypeDefinition("functions")` added to schema; registrar subscribes to `work.task.completed` |
| MVP-FN-005 | Function Registry & Dispatch | 2026-05-20 | main | `internal/functions`: `Registry` interface + impl; `RegisterAll` wires `work.task.completed → handleWorkTaskCompleted`; `FunctionsDispatcher` starts→completes/fails Job end-to-end; `EventReceiverServiceServer` registered in app |
| MVP-FN-006 | Job gRPC API | 2026-05-20 | main | `ListJobs`, `GetJob`, `CancelJob` RPCs in proto + server; `JobFilter` type; routes registered with Cross; 17 unit tests pass |
| FEAT-20260602-001 | `start-pipeline` function — mints WorkflowRun on `work.pipeline.requested`, publishes `work.pipeline.started` + `work.next.requested` | 2026-06-02 | feature/Dev-FUNCTIONS-FEAT-20260602-001_start_pipeline_function | New `functions/start-pipeline` Python binary + `functions/start-pipeline.json` manifest; POSTs `/work/{agency}/workflow-runs` (forwards optional `name`/`trigger_event`/`initiator` — server generates name when empty), then publishes confirmation (`work.pipeline.started` carrying the new `workflow_run_id`) and handoff (`work.next.requested`) via `/pubsub/{agency}/events`. Registrar now subscribes to `work.pipeline.requested` so Cross pushes the event to `NotifyEvent`/dispatcher. Smoke-tested missing-env + unreachable-Cross paths; build/vet/tests green. |
| FEAT-20260602-004-FUNCTIONS | CodeValdFunctions leg of WorkflowRun rollback — `FunctionsService.RollbackByWorkflowRun` cancels in-flight Jobs / freezes terminal Jobs as `rolled_back` audit | 2026-06-02 | feature/Dev-FUNCTIONS-FEAT-20260602-004_rollback-by-workflow-run | `models.go`: `JobStatusRolledBack` + `RollbackByWorkflowRunResult`. `schema.go`: `rollback_reason` property on Job. `errors.go`: `ErrWorkflowRunIDRequired`. `events.go` (new): `TopicJobCancelled`/`TopicJobRolledBack` + `JobCancelledPayload`/`JobRolledBackPayload` (carry `previous_status` so consumers distinguish completed-vs-failed audit freezes without a refetch). `functions.go`: new `RollbackByWorkflowRun` interface method. `workflow_run_rollback.go` (new): manager impl — pending/running/retrying → cancelled (emits `functions.job.cancelled`); completed/failed → rolled_back (emits `functions.job.rolled_back`); cancelled/rolled_back → skipped (idempotent). `applyRollbackStatus` preserves `completed_at` if already set + leaves `result`/`error`/`retry_count` untouched so debugging artefacts survive. `proto/codevaldfunctions/v1/service.proto`: `RollbackByWorkflowRunRequest`/`Response` + RPC; `make proto` regenerated. `internal/server/server.go`: handler — `ErrWorkflowRunIDRequired` → `INVALID_ARGUMENT`. `internal/registrar/registrar.go`: `DELETE /functions/{agencyId}/by-workflow-run/{workflowRunId}` route + `functions.job.cancelled`/`functions.job.rolled_back` added to published topics. 10 domain tests (`workflow_run_rollback_test.go`) + 3 handler tests (`internal/server/workflow_run_rollback_server_test.go`). `go build` ✅, `go vet` ✅, `go test -race ./...` ✅. |
| FEAT-20260602-002 | `workflow_run_id` on `Job` + every `functions.job.*` event payload (Functions sibling of [Cross umbrella](../../../CodeValdCross/documentation/3-SofwareDevelopment/mvp-details/FEAT-20260602-001_workflow_run_id_propagation_umbrella.md)) | 2026-06-02 | feature/Dev-FUNCTIONS-FEAT20260602002_workflow_run_id_in_functions | `schema.go`: `workflow_run_id` property on Job. `models.go`: `Job.WorkflowRunID`; `JobFilter.WorkflowRunID`. `converters.go`: hydrate from entity. `functions.go`: `CreateJob` signature gains `workflowRunID`; new `SetJobWorkflowRunID` for the start-pipeline back-fill case. `manager.go`: persist on create, propagate into every `functions.job.*` event payload, filter on `workflow_run_id` in `ListJobs`. `proto/codevaldfunctions/v1/service.proto`: `Job.workflow_run_id` (field 14), `JobFilter.workflow_run_id` (field 3); `buf generate`. `internal/server/event_receiver.go`: dispatcher extracts `workflow_run_id` from trigger payload and passes to `CreateJob`; if result JSON of a function carries `workflow_run_id` (start-pipeline case), back-fills via `SetJobWorkflowRunID`. `internal/server/server.go`: `ListJobs` forwards `workflow_run_id` filter; `jobToProto` includes the field. Tests: 5 new manager-level tests (persistence, empty allowed, list filter, back-fill, publish payload) + `internal/server/dispatcher_test.go` for `extractWorkflowRunID`. `go build` ✅, `go vet` ✅, `go test -race ./...` ✅. |
